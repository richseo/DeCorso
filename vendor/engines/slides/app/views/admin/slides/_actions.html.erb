<ul>
  <% if Admin::SlidesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_slides_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_slide_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::SlidesController.sortable? and Slide.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Slides"),
                 admin_slides_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Slides"),
                 admin_slides_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>