<% content_for :body_content_left do %>
  <ul id="links">
    <% @links.each do |link| %>
      <li>
        <%= link_to link.title, link.url %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
